$(document).ready(function () {
  // after()
  // $('h1.title:last').after('<h2>New Title after</h2>');

  // before()
  // $('.container h1.title:last').before('<h2>New Title before</h2>');

  // append()
  // $('ul[data-type="list"]').append('<li>New Item appended</li>');

  // appendTo()
  // $('<p>Appended to container</p>').appendTo('.container');

  // prepend()
  // $('ul[data-type="list"]').prepend('<li>New Item appended</li>');

  // prependTo()
  // $('<li>Prepended to container</li>').prependTo('ul[data-type="list"]');

  // replaceWith()
  // const newContent = '<h2>New replaced content</h2>';
  // $('.title').replaceWith(newContent);

  // replaceAll()
  // const newElement = $('<h3>New replaced content</h3>');
  // $(newElement).replaceAll('.title');


  // html() - SPECIAL -> get & set
  // const newHTML = '<p>New HTML content</p>';
  // $('.container').html(newHTML);
  // let containerHtml = $('.container').html();
  // console.log(containerHtml);

  // text() - SPECIAL -> get & set
  // const newText = 'New text content 2';
  // $('.container').text(newText);
  // $('.title:first').text("NEW TITLE ONE");

});
