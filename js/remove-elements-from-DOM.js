$(function () {
  // Remove the second title with class "title"
  // $('.title:eq(0)').remove();

  // Detach the paragraph with id "paragraph"
  // const paragraph = $('#paragraph').detach();
  // console.log(paragraph);
  // $('body').append(paragraph)

  // Empty the unordered list with data-type "list"
  // $('ul[data-type="list"]').empty();
});
